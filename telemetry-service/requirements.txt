fastapi==0.141.1
pydantic==2.13.5
pydantic-settings==2.15.0
pymongo==4.17.0

# The Inference Service is called over HTTP from the ingest path, so httpx is a
# runtime dependency of this service, not only a test one.
httpx==0.28.1
uvicorn==0.41.0

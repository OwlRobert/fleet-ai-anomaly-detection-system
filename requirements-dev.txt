# Test dependencies for both services.
#
# Install the service requirements alongside these to run the suites:
#   pip install -r telemetry-service/requirements.txt \
#               -r inference-service/requirements.txt \
#               -r requirements-dev.txt
#
# httpx is also required by starlette.testclient, so it stays listed here for
# the Inference Service, whose application code does not use it. The Telemetry
# Service now depends on it at runtime and lists it in its own requirements.
pytest==9.1.1
httpx==0.28.1

# The MongoDB integration tests are skipped unless a test instance is pointed at:
#   TEST_MONGODB_URI=mongodb://localhost:27017 python -m pytest
# They use their own database and drop it afterwards. Async tests run on anyio's
# pytest plugin, which arrives with FastAPI, so no extra async plugin is needed.
